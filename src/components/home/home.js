import React from 'react';
import PageHeader from '../shared/page-header';

const Home = () => {
    return (
        <div className="container-fluid">
            <PageHeader>You are home!</PageHeader>
        </div>
    );
};
Home.propTypes = {};

export default Home;
